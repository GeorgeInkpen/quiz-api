version: 0.2
phases:
  install:
  pre_build:
    commands:
    - npm install
    - npm fund
  build:
    commands:
    - npm build .
    - npm test
artifacts:
  files:
    - src/**/*
    - package.json
